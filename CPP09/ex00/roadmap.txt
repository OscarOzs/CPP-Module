ROADMAP :

- Parsing :
	- fichier invalide (inexistant ou inacessible)
- Parsing data.csv :
	- stocker toutes les lignes dans std::map

Parsing de input.txt :
	- Bad input :
		- mauvais format
		- date entre ceux de data.csv
		- mois < 12 && mois > 1
		- jours < 1 && jours > 31
	- Too large a number :
		- nombre > 1000
	- Not a positive number :
		- valeur negatif


- Main algo :
	- chercher date correspondante, si inexistante : date precedente
	- afficher sur output pur chaque ligne ou message d'erreur si ligne invalide

- calculate :
	- fonction pour savoir si la clef existe.
		- si elle existe : print le res
		- si elle existe pas : la creer, decrementer l'iterateur, supprimer l'ancienne
		puis print le res.
	